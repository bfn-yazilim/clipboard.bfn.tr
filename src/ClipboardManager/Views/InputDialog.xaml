<Window x:Class="ClipboardManager.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Girdi"
        Width="380" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource SurfaceBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="PromptText"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,10" />

        <TextBox Grid.Row="1"
                 x:Name="InputBox"
                 FontSize="14"
                 Padding="8,6"
                 KeyDown="InputBox_KeyDown" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="İptal" Width="84" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True" />
            <Button Content="Tamam" Width="84" Click="Ok_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
